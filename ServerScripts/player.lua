-- function Overload(unit)
--     unit.mp.set(0)
--     unit.AddBuff(3)
--     unit.SendUpdated()

--     if (unit.UseSkill(0, null, false) ~= nil) then
--         unit.mp = unit.mp+1
--     end
-- end

-- Server.OnJoinPlayer(unit).Add(Overload)

-- unit.mp = 0
-- unit.AddBuff(3)
-- unit.SendUpdated()

